package pos_delivery.view.Scene;

import javafx.scene.Scene;
import javafx.stage.Stage;

public class MenuLayoutScene extends SceneBase {

    public MenuLayoutScene(Stage primaryStage, Scene scene, boolean enableStyle) {
        super(primaryStage, scene, enableStyle);
    }
}
